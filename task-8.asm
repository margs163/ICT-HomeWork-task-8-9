	INP
	STA DIV
	INP
	STA COUNT
	LDA ZERO
	STA RESULT

	LDA COUNT
	BRZ NULL	
	LDA DIV
	BRZ NULL
	BRP LoopPos
	BRA LoopNeg 


LoopPos LDA COUNT
	BRP PosC
	BRA NegC
	 	
PosC	LDA RESULT
	ADD ONE
	STA RESULT
	LDA DIV
	SUB COUNT
	STA DIV
	BRZ NULL
	BRP LoopPos
	BRA NULL

NegC	LDA RESULT
	SUB ONE
	STA RESULT
	LDA DIV
	ADD COUNT
	STA DIV
	BRZ NULL
	BRP LoopPos
	BRA NULL


LoopNeg LDA COUNT
	BRP NPosC
	BRA NNegC
	 	
NPosC	LDA RESULT
	SUB ONE
	STA RESULT
	LDA DIV
	ADD COUNT
	STA DIV
	BRP NULL
	BRA LoopNeg

NNegC	LDA RESULT
	ADD ONE
	STA RESULT
	LDA DIV
	SUB COUNT
	STA DIV
	BRP NULL
	BRA LoopNeg


NULL	LDA RESULT
	OUT
	HLT

DIV	DAT
COUNT	DAT
RESULT	DAT
ZERO	DAT 0
ONE	DAT 1
